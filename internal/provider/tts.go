package provider

import sonata "github.com/omeyang/Sonata/engine/aiface"

// TTSConfig 持有 TTS 调用的配置（Sonata 类型别名）。
type TTSConfig = sonata.TTSConfig

// TTSProvider 将文本合成为语音（Sonata 类型别名）。
type TTSProvider = sonata.TTSProvider
